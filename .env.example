# Copy this file to .env and fill in your own local PostgreSQL credentials.
# .env is git-ignored and should never be committed with real values.

DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=customer_behavior